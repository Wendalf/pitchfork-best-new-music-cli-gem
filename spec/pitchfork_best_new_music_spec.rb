require 'spec_helper'

describe PitchforkBestNewMusic do
  it 'has a version number' do
    expect(PitchforkBestNewMusic::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
